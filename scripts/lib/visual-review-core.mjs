import { createHash } from 'node:crypto';
import { existsSync, lstatSync, readFileSync, realpathSync } from 'node:fs';
import { resolve } from 'node:path';
import { validate } from './json-schema-lite.mjs';
import { listFeedbackFiles, validateFeedbackFile } from './registry-core.mjs';
import { VISUAL_FAILURE_ACTIONS, expectedVisualFailureAction } from './visual-job-core.mjs';
const ROOT = resolve(new URL('../..', import.meta.url).pathname);
const schema = (name) => JSON.parse(readFileSync(resolve(ROOT, 'schemas', name), 'utf8'));
export const REVIEW_CODES = Object.freeze({ FEEDBACK_INVALID:'visual-review-feedback-record-invalid', SCHEMA:'visual-review-schema', DIMENSIONS:'visual-review-dimensions', ROUTE:'visual-review-route', FEEDBACK:'visual-review-feedback-route-invalid', FEEDBACK_UNRESOLVED:'visual-review-feedback-unresolved', FEEDBACK_SUBJECT:'visual-review-feedback-subject', FEEDBACK_BASIS:'visual-review-feedback-basis', FEEDBACK_OWNER:'visual-review-feedback-owner', FEEDBACK_MODALITY:'visual-review-feedback-modality', FIXTURE_PATH:'visual-fixture-path', FIXTURE_HASH:'visual-fixture-hash', POSITIVE:'visual-positive-slot-invalid' });
const tagClass = Object.freeze({ dashboardization:'dashboardization', ui_mimicry:'dashboardization', flat_svg_aesthetic:'dashboardization', generic_icon_grid:'dashboardization', box_overload:'dashboardization', over_minimalization:'wrong_concept', weak_visual_thesis:'wrong_concept', style_dilution:'wrong_concept', dense_text:'facts_or_text_wrong', reference_drift:'reference_drift', factual_overlay_intrusion:'facts_or_text_wrong' });
export function expectedVisualReviewRoute(tag) { const failure_class = tagClass[tag]; return failure_class ? { failure_class, next_action: expectedVisualFailureAction(failure_class) } : null; }
export function validateVisualReview(record, { feedbackDir } = {}) { const out = validate(record, schema('visual-review.schema.json')).map(e=>({code:REVIEW_CODES.SCHEMA,message:e.message})); if(out.length) return out; const required=['thesis_clarity','reading_path','narrative_composition','editorial_authorship','spatial_richness','information_hierarchy','article_fit','reference_adherence','brand_compatibility','factual_text_integrity','mobile_crop_resilience']; if(new Set(record.dimensions.map(x=>x.dimension)).size!==required.length||required.some(x=>!record.dimensions.some(d=>d.dimension===x))) out.push({code:REVIEW_CODES.DIMENSIONS,message:'eleven dimensions exactly once'}); const route=expectedVisualReviewRoute(record.primary_tag); if(record.verdict==='PASS_TO_HUMAN_REVIEW' ? (record.primary_tag!==null||record.next_action!=='human_judgement') : (!route||route.failure_class!==record.failure_class||route.next_action!==record.next_action)) out.push({code:REVIEW_CODES.ROUTE,message:'verdict/tag route mismatch'}); const fbPath=listFeedbackFiles(feedbackDir).find(p=>{try{return JSON.parse(readFileSync(p,'utf8')).feedback_id===record.feedback_ref}catch{return false}}); const fb=fbPath?JSON.parse(readFileSync(fbPath,'utf8')):null; if(!fb) out.push({code:REVIEW_CODES.FEEDBACK_UNRESOLVED,message:'feedback_ref is unresolved'}); else { // The referenced record must be a valid feedback record, not merely JSON // that happens to carry the right four fields.
    const fbIssues=validateFeedbackFile(fbPath); if(fbIssues.length) out.push({code:REVIEW_CODES.FEEDBACK_INVALID,message:`referenced feedback record is invalid: ${fbIssues[0].message ?? fbIssues[0].code}`}); if(fb.subject?.ref!==record.asset_ref)out.push({code:REVIEW_CODES.FEEDBACK_SUBJECT,message:'feedback subject differs'}); if(fb.basis!=='model_inference')out.push({code:REVIEW_CODES.FEEDBACK_BASIS,message:'feedback basis differs'}); if(fb.owner_verdict!=='unknown')out.push({code:REVIEW_CODES.FEEDBACK_OWNER,message:'feedback owner verdict differs'}); if(!fb.routing?.abstained&&fb.routing?.modality_layer!==record.primary_tag)out.push({code:REVIEW_CODES.FEEDBACK_MODALITY,message:'feedback modality differs'}); } return out; }
export function validateVisualFixture(fixture) { const out=validate(fixture,schema('visual-negative-fixture.schema.json')).map(e=>({code:REVIEW_CODES.SCHEMA,message:e.message})); if(out.length)return out; if(fixture.status==='positive_pending_owner_input') { if(fixture.asset_ref!==null||fixture.asset_sha256!==null||fixture.owner_judgement!==null||!String(fixture.blocking_reason).includes('SUE-643')) out.push({code:REVIEW_CODES.POSITIVE,message:'positive slots are inert pending SUE-643'}); return out; }
  // An owner-positive slot is only meaningful with a real asset, an owner
  // acceptance, and a review record to compare against. Anything less is a
  // pending slot, not a positive reference.
  if(fixture.status==='owner_positive'&&(fixture.owner_judgement!=='accepted'||!fixture.asset_ref||!fixture.asset_sha256||!fixture.review_record)) out.push({code:REVIEW_CODES.POSITIVE,message:'owner_positive requires an accepted owner judgement, an asset, a digest, and a review record'});
  if(fixture.status==='negative'&&fixture.owner_judgement==='accepted') out.push({code:REVIEW_CODES.POSITIVE,message:'a negative fixture cannot carry an accepted owner judgement'}); try { const p=resolve(ROOT,fixture.asset_ref); if(!p.startsWith(`${ROOT}/`)||!lstatSync(p).isFile()||lstatSync(p).isSymbolicLink()||!realpathSync(p).startsWith(`${ROOT}/`)) throw new Error(); const h=`sha256:${createHash('sha256').update(readFileSync(p)).digest('hex')}`; if(h!==fixture.asset_sha256) out.push({code:REVIEW_CODES.FIXTURE_HASH,message:'asset hash mismatch'}); } catch { out.push({code:REVIEW_CODES.FIXTURE_PATH,message:'asset must be repository-contained regular file'}); } return out; }
 export { VISUAL_FAILURE_ACTIONS };
